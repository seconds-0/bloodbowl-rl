**Current status — September 6, D368–D370:** local four-arm training is running.
The first run has completed 256 updates; the second is active. The live checklist
is the authoritative progress display. After all four runs pass independent
verification, evaluate every model in the predeclared 512-game comparison.
A separate eight-match development diagnostic is being prepared; it cannot
select checkpoints or exclude a model from the full-game exam.

The added 32 matches contain 119 near-end-zone own-carrier records, versus 280
in the original 32. At the fixed repeat counts, the diverse arm sees fewer such
presentations. This comparison therefore tests unconditional match diversity,
not increased finishing exposure. Preserve its inputs and interpret that limit.

Additional Vast spend remains **$0**. Do not fund a larger run until the current
recipe demonstrates reproducible full-game scoring and match-utility gains.
The estimated four-run local training cost is about 39 CPU minutes; measured
wall time and exam results will replace that estimate after completion.

The entries below are historical snapshots, newest first. Statements about
whether a run was active apply to their recorded stage, not the current state.

**Live — September 6, D368:** the four-arm data-diversity comparison is now
running on local CPU under the frozen, independently reviewed plan. First arm
has emitted finite updates. Each arm receives 256 full-match optimizer updates:
control 183,600 decision presentations; diversity 189,664. Paired seeds are
20260909 and 20260910 with counterbalanced arm order. Estimate about 39 minutes
combined training; actual cost and performance remain pending. The live HTML
checklist reads actual progress every ten seconds. All four accepted models
will play the frozen full-game exam. Additional Vast spend: **$0**.

**Current — September 6, D365–D366:** the additional 32 unconditional teacher
matches are generated and independently replay-verified. The merged corpus has
64 complete sequences and 47,416 unique decisions. Control exposure is 183,600
decisions; diversity exposure is 189,664, with 256 optimizer updates per arm.
Fresh paired initializations for seeds 20260909/10 are independently qualified.
No training has launched yet: final supervisor/freezer integration and prelaunch
review remain. Four fits are estimated at about 39 local CPU minutes. All four
accepted trained models will play the predeclared full-game exam regardless of
DEV imitation accuracy. Additional Vast budget remains **$0**.

**Preparation update — September 6, D360–D361:** independent training-data
inspection finds only 280 of 22,950 decisions with our carrier within six
squares of the end zone, across five of 32 matches. This is a coverage
diagnostic, not proof of causation. Prepare a fixed-update comparison of the
original 32 matches against a nested 64-match set, with the exact same accepted
teacher and model. Each arm receives 256 complete-match optimizer updates;
decision-count differences will be reported explicitly. Four local fits are
estimated at about 39 CPU minutes from measured prior runs, subject to resource
qualification. No new corpus generation or training has launched. Vast budget
remains **$0**. The teacher redesign stays a separate experiment.

**Current — September 6, D359:** the four-seed report is repaired and independently
verified. Across 512 games per group, fresh policies achieved W/D/L 0/175/337,
TD 0/543; trained policies achieved 2/195/315, TD 2/454. The aggregate utility
gain does not override the failed fresh-seed scoring and cage-opponent gates.
No training is currently running. Next local work qualifies policy-visible
teacher labels before a separately frozen generalization experiment. Keep
additional Vast spend at **$0**. No checkpoint or reward is promoted.

**Latest — September 6, D356–D358:** both fresh-seed replications completed,
using 19.47 combined local CPU training minutes. All512 new full-game exam
results pass independent integrity verification. Neither trained seed scored a
touchdown. Paired match utility improved by0.78 and2.34 percentage points, but
the combined cage-opponent result fell0.39 points. The fixed scoring and
opponent confirmation gates fail. Additional Vast budget remains **$0**.

The four-seed report needs a separate compatibility repair for the older
controller API; frozen runs and raw games are preserved. Next local work will
target generalization and coverage rather than fund a larger run of this
unconfirmed recipe. The earlier negative seed remains in all reporting.

# Training and funding plan after the model-facing audit

> Current D355: fresh-seed replication is running locally at seeds 20260907/08 with the unchanged eight-epoch recipe. Plan `0055006f…` binds both runs and a new 512-game exam before training. Estimated 19.2 combined CPU training minutes; actual outcome pending. The first screen gained 3.125 pp aggregate utility but had opposite seed signs, and DEV exact action fit is only ~51% versus ~81% on train. Additional Vast spend: $0.

> Current D352: full-match BC scored 2 TD and won 2/256 games; fresh scored/won 0. Combined match utility +3.125 pp, with opposite training-seed signs. Independent verification accepts all 512 natural games. Next: unchanged-recipe replication with two new initializations and unused kickoff seeds, estimated 19.2 combined CPU training minutes. No trainer is currently running. Additional Vast spend remains $0; paid scaling and promotion are not justified.

> Current D351: both full-match BC seeds accepted after independent trace/checkpoint and train-fit verification. Measured total training time 19.20 local CPU minutes; exact training action agreement approximately 81% for both. Fixed 512-game kickoff evaluation is running under plan `30b82038…`. Vast spend $0; strength remains unproven until this exam and fresh confirmation.

> Live update 2026-09-07T01:59:31.833531+00:00: first full-match BC arm completed 256 updates / 183,600 decisions in 581.11 training seconds, checkpoint `10561b25…`. The frozen supervisor accepted its artifacts and launched seed 20260906 (PID 91018). Independent two-arm verification and the predeclared 512-game comparison remain pending. Additional funded compute: $0.

**Live — September 6, D350:** the two-seed full-match teaching screen is running
on local CPU. Each model receives the same 32 complete contact-opponent teacher
games for eight epochs: 256 updates and 183,600 decision presentations. All
source-seed families are isolated from development and test data. The longest
actual train-sequence smoke took 3.75 seconds / 633 MB; plan roughly 20–25 minutes
for both arms, subject to the 30-minute per-arm cap. Full-game performance is
unknown until the frozen 512-game comparison completes. Additional Vast spend: $0.


Active iteration — September 6, D346–D347: a new 32-game discovery diagnostic
finds that BC holds the ball in 13/16 games but never brings an observed carrier
within 12 squares of the end zone. All 651 activation declarations are Move.
This supports a broader-demonstration test; it does not prove which alternative
action would have been correct. Full-game teacher recording and a streamed
full-gradient BC runner are being qualified. No new training arm has launched.

A train-only synthetic resource probe retained full recurrent gradients through
2,048 decisions in 6.59 seconds at 793 MB process peak memory on the Mac CPU.
Actual natural-match training sequences must still be benchmarked before the
run budget is frozen. Additional Vast spend remains **$0**.


Latest result — September 6, D345: the 512-game fresh-versus-BC kickoff transfer
is complete and independently verified. Neither fresh nor trained models scored
a touchdown. Both groups finished with exactly 0 wins, 92 draws and 164 losses
across 256 games each. The earlier authored-position improvement is real but
has not transferred to full-game scoring. Keep additional Vast spend at **$0**.
The next local improvement is broader demonstration coverage from ball
acquisition through drive progression, followed by new held-out full-game tests.


Execution update — September 6: the fresh four-run memory comparison has
completed and passed independent artifact validation (D338). It produced
999,817,216 aligned training steps in 1h43m32.869s of unit wall time on the local
RTX 2070 at a fixed 125W power limit. All 16 integrity counters are zero;
maximum recorded temperature is 81°C. The earlier thermal failure remains
rejected and is excluded from these measurements.

The separate frozen 512-game held-out exam is complete and independently
accepted (D339). All four models scored zero touchdowns. Historical seeds
42/43 recorded W/D/L of 0/50/78 and 0/47/81; corrected seeds 42/43 recorded
0/46/82 and 0/48/80. Corrected-minus-historical normalized match utility is
−1.5625 percentage points for seed 42 and +0.390625 for seed 43, averaging
−0.5859375 points. The inconsistent signs and absent scoring provide no useful
learning-performance ranking. The correctness repair remains qualified.

The complete exam took 24m57.843s of unit wall time, or 2.925 seconds/game
including validation. Maximum temperature was 62°C, with all integrity checks
passing. These are full kickoff games against two scripts, not a learned-opponent
or championship evaluation. D340 therefore selects the prepared next local
experiment: fixed 25-epoch whole-trajectory behavioral cloning in the real
H512/L3 recurrent policy, compared with exact fresh initialization on 128
unseen-position natural-dice trials. That experiment has completed and passed independent verification (D342).

**Current funding recommendation: add $0 to Vast for this stage.** Local compute
is sufficient to answer the next learnability question. More scratch self-play
steps are not yet supported by the observed scoring result. The current screen
costs about 103.55 minutes per billion aligned steps end to end on this rig;
this includes validation and embedded evaluation overhead, so it is not a raw
training throughput claim. A linear planning estimate for another identical
four-arm screen is roughly 1h44m, subject to the same thermal and integrity
guards. This measurement does not predict another GPU's speed or rental price.

The exact screen contract is in
[memory-learning-screen-2026-09-05.md](memory-learning-screen-2026-09-05.md).
The live checklist separates completed training, running match evaluation,
prepared teaching experiments, and funding decisions.


## Measured teaching result and next funding gate — September 6

The actual main recurrent policy learns a limited but useful scoring skill from
whole-trajectory demonstrations. Both seeds trained for exactly 25 epochs on
56 trajectories (420 decisions), from their original pinned initial weights.
The accepted CPU runs took 101.84 and 94.70 seconds, or **3m16.54s total**.
Earlier report-writer and initial-file-copy failures remain rejected and are
not included in that accepted-run timing. Their fixes and preserved evidence
are recorded in D341.

| Training seed | Fresh TD / trials | BC TD / trials | New geometry | Held-out side/family |
| --- | ---: | ---: | ---: | ---: |
| 20260905 | 0 / 32 | 11 / 32 | 4 / 12 | 7 / 20 |
| 20260906 | 0 / 32 | 6 / 32 | 2 / 12 | 4 / 20 |
| Combined | 0 / 64 | 17 / 64 | 6 / 24 | 11 / 40 |

All 128 trials use natural dice and exact conditional action masks; all
illegal-action, engine-error and projection-collision outcomes are zero.
Common starting-state fingerprints and matched trial keys are verified.
Training accuracy (97.62% and 99.29%) is a separate measurement. The result
shows authored-position completion, with only eight unseen positions and four
dice seeds. Demonstration continuations used favorable scripted dice, and the
32-decision trial cap prevents a full-match claim. Do not interpret the combined
26.56% completion rate as a win rate or as 64 independent starting positions.

**Full-game transfer is now complete (D345).** The exact four Torch checkpoints
played 128 games each against contact and cage scripts, on both sides, with
common kickoff seeds. No conversion or reserialization occurred. The adapter
passed exact policy-path and production-step comparisons before the exam.

| Training seed | Fresh W / D / L | BC W / D / L | Fresh TD for / against | BC TD for / against |
| --- | --- | --- | --- | --- |
| 20260905 | 0 / 41 / 87 | 0 / 40 / 88 | 0 / 139 | 0 / 132 |
| 20260906 | 0 / 51 / 77 | 0 / 52 / 76 | 0 / 120 | 0 / 125 |
| Combined | 0 / 92 / 164 | 0 / 92 / 164 | 0 / 259 | 0 / 257 |

BC-minus-fresh match utility differs by −0.390625 percentage points in one seed
and +0.390625 in the other. The aggregate difference is exactly zero. An isolated
contact/home improvement is offset by other cells; it does not justify selecting
that favorable result. All 512 games ended naturally with zero integrity counters.
Caps are failures, never draws. The independently checked producer, raw rows,
256 paired differences, runtime identities and complete source closure are in
`audit-artifacts/bc-kickoff-transfer-20260906/`.

The new direct Torch/production-C serial path completed the entire exam in
**65.46 seconds on Mac CPU**, including startup and validation—about 7.82 games
per second for this specific configuration. The earlier native exam's 24m58s
is a different evaluation path, not a benchmark of the same workload or a
hardware-speed comparison. This makes repeated small local transfer exams cheap.

**Recommended next local tranche: broaden the demonstrations.** All 64 existing
trajectories start with a ball carrier near the end zone (distance families 3–7)
and contain 5–9 decisions. That distribution can teach finishing without teaching
how to reach those situations. The full-game outcome supports testing this
coverage hypothesis; it does not identify the precise in-game failure phase.

Prepare current-engine demonstrations spanning ball acquisition, protected
advance, and finishing, including both coaches, varied score/clock situations,
and natural-risk continuations. Keep complete source matches/trajectories and
geometry families together in train or holdout, and reserve new holdouts for
selection. Compare a fixed training budget with the existing finishing-only
curriculum from matched initial weights. Measure stage completion separately,
then evaluate complete kickoff games with W/D/L and TD for/against as the
acceptance criteria. Do not reward human-looking action rates or increase epochs
on the same tiny corpus based on these test results.

If broader experience produces consistent full-game gains in both seeds, run a
local replication and held-out learned-opponent/roster checks before proposing
paid scale. No such gain or promotion has been established yet.

**Funding decision now: $0 additional Vast budget.** A future paid proposal
must identify a locally supported question, benchmark the intended rented GPU,
and freeze a concrete spend ceiling before launch. Calculate total hours from
training steps/useful throughput plus full-game evaluation time and measured
setup overhead; multiply by the actual quoted hourly rate and include storage.
Stop at either the runtime or spend ceiling, preserve artifacts, then release
capacity. No current rental-price estimate or paid authorization is implied.
Local acceptable play and championship/FUMBBL readiness remain later goals.

Evidence: `audit-artifacts/main-recurrent-teaching-recovery-v2-20260906/ROOT_MATCHED_BC_VERIFIED.json`
and its bound training/evaluation records. D342 is the accepted interpretation.

## Recurrent-memory evidence update — September 5, D329–D334

The terminal-aware memory contract is implemented and passes real CPU and CUDA
qualification. The old trainer leaked recurrent state across completed games
inside a rollout window. The replacement clears those boundaries and preserves
detached numeric state across ordinary H64 windows, with coherent PPO initial
state gathering, terminal masking and copied tail evaluation. Native direct
derivative error is below8.36e-8; the focused PPO ratio residual is below9.54e-7.
Recorded counters prove graph and eager execution took their intended paths;
all saved arrays match exactly. H512/L3 LR0 throughput is100,758 steps/sec.

A frozen-checkpoint bridge completes16 games per runtime across two opponents,
both sides and two seeds. All16,913 paired decisions match exactly, including
observations, exact legal support, sampled actions, log probabilities and values.
All16 integrity counters are zero and serialized weights remain unchanged.
The full eight-bank gate also passes: 475,136 ratios, all 1,072 primary rows,
maximum ratio error 9.06e-6 below 2e-5, unchanged LR0 weights, and zero integrity
counters. The clean sustained canary completes 49,938,432 training steps,
163,235 training games and 10,175 evaluation games with all 16 hard fields zero.
Its checkpoint reloads and saves byte-identically in a fresh GPU constructor.
The entire measured producer contract matches without an audit amendment.

Final evaluation is all draws and zero touchdowns. This completes memory and
execution qualification; controlled learning and held-out match-strength tests
remain future experiments. The earlier canary with a monitor interval mismatch
remains rejected. No paid GPU use, production change or promotion occurred.

Evidence: `audit-artifacts/memory-integration-20260905/`,
`audit-artifacts/memory-canary-repeat-20260905/` and D329–D334.


The next useful investment is a trustworthy, measurable game-learning loop.
More GPU steps cannot teach an option that is absent from the legal list,
restore information missing from observations, or correct a simulator that
implements different consequences. Keep paid Vast work paused while the local
RTX 2070 closes these gates. This is a sequence of bounded experiments, not a
commitment to implement every future idea before measuring anything.

## Earlier execution and storage recovery gates (D324–D328)

The user-approved managed WSL move to D: is complete. Windows registration,
physical capacity, Linux read/write access, and the frozen runtime/source and
saved checkpoint identities were independently verified. C: recovered about
277 GB of free space; D: had about 1.34 TB free after relocation. D324 records
the recovery and remaining diagnostic details.

Every new rig operation checks the physical backing volume before and during
execution, with a 100 GiB reserve plus a declared allocation allowance. Guest
free space alone is insufficient. The rebuilt runtime shares the immutable
Python environment instead of copying its 8 GB of packages.

Complete native startup exposed two additional harness defects: a relative
Puffer JSON-log output path inside the immutable runtime, and a final metric
reducer that tried to average text-valued entropy diagnostics. Explicit output
routing and separate text metadata now have focused regressions. The isolated
rebuild passes installed-source verification; actual native execution exits
successfully and writes results. The clean native startup is accepted: 765 completed evaluation games, all 16
integrity counters zero, successful process exit and complete saved outputs.
Generic numerical qualification and the corrected eight-bank repeat pass.
Root independently verified all raw ratios and all eight input sidecars. The
reviewed v9-r2 launch package passed19 supervisor tests, and the disposable
50M-step local canary has completed:49,938,432 aligned training steps,146,099
training games and10,194 eval games, all16 integrity counters zero, complete
saved results and byte-identical checkpoint reload. It scored no touchdowns;
this is integrity acceptance only. A redundant plan metadata label required an
explicit audit amendment; actual prelaunch and executed identities agree.
No failed run is accepted, and no paid training has started.

## 1. Complete the current runtime acceptance sequence

The corrected obs-v7 source (including Blitz–Stab context) is frozen and its isolated fp32 extension passes
the unchanged eight-bank gate: maximum ratio residual 9.54e-6 below 2e-5,
all 557,056 raw ratios checked, unchanged weights and all 16 integrity fields
zero. The separate CPU viewer passes six natural games and 6,550 legal-support
checks. Actual module/source/backend/patch/ABI identities are preserved in
D316 and the corrected-runtime evidence bundles. The frozen neural baseline is complete (256 games); the bounded numerical
training canary is complete (D328). Memory correctness is now qualified under D329–D334. Playing strength and
the remaining choice/rule gaps remain separate gates.

Current source evidence: 616 native cases and the writer check pass normally
and under ASan/UBSan; 100 standalone self-test episodes pass. The final seven
converter tests execute under unittest discovery. Historical numerical evidence
from obs-v6 remains useful but cannot substitute for this v7 build.

Use separately zero-extended v6 checkpoints only for qualification/evaluation.
They require original hash-bound v6 sidecars and newly manifested, ineligible
v7 sidecars. Do not rewrite old files or infer lineage from weight size. Prove
copied tensor bytes and measure actual forward differences. Passing these
checks does not automatically make a migrated checkpoint training-eligible.

## 2. Make the common decision mechanics learnable

The 108-ID registry now has source/rules coverage; many findings still need
executable witnesses. The prioritized backlog is the accompanying JSON/Markdown
under `audit-artifacts/model-facing-audit-20260904/integration-v7/` and is visible
on the live checklist. Start with shared machinery that affects many games:

1. Route eligible dice through explicit reroll windows, preserving owner,
   eligible sources, decline, roll-use restrictions and parent continuation.
   This covers missing TTM/KTM and direct-special rerolls, Pro and Steady Footing.
2. Correct common ball/resource semantics, including Catch/Sure Hands usage,
   Leader lifetime, and Active-skill suppression while Distracted.
3. Complete Block/Push and Foul/send-off choices: Juggernaut, Grab, Brawler,
   damage-modifier allocation, offensive-assist selection and Argue/Bribe order.
4. Repair remaining activation/movement choices and partially implemented traits,
   then complete missing whole actions such as Bombardier and Ball & Chain.

Each accepted repair needs a failing witness before the fix, both coach
orientations, genuine legal-trajectory reachability where applicable, exact
joint-action round trips, public decision context, injected-dice outcomes,
resource accounting and continuation checks. Track source inspection separately
from executed proof. Test interactions that change eligibility, such as the
new My Ball/Dump-off regression, rather than only isolated skill use.

A bounded experiment may use an explicitly audited mechanic scope; it must
record unsupported mechanics and make no full-BB2025 claim. Starting-roster
filtering alone cannot enforce that scope because procgen can award learned
skills. Scope enforcement must cover generated players, state banks, opponents
and replay examples, and must reject unsupported content explicitly.

## 3. Measure learning after the memory repair

D329–D331 close the confirmed episode-boundary memory leak and qualify the
terminal-aware-tbptt-v1 implementation. The old trainer also deliberately reset
memory at every H64 rollout boundary; the new trainer carries detached state.
The old zero-state tail was consistent with its old reset regime. These are
separate facts: a proven episode leak and an explicit finite-context change.
The frozen-evaluation bridge is exact across16,913 paired decisions.

After the disposable canary passes, freeze a common-initialization learning
comparison with only the memory contract changed. Keep reward, opponent mixture,
optimizer, H64 gradient horizon, seeds, evaluation policy and total transitions
fixed. The historical control stays isolated and diagnostic. Evaluate held-out
full games on both sides; do not promote from a smoother training curve.

Only then compare H64 with one longer gradient horizon as its own declared
factor. Median possession-to-score delay of146.5 engine decisions motivates
that question but does not establish the answer. The full proposed contracts
are in [the recurrent-memory tranche plan](recurrent-memory-next-tranche-2026-09-05.md).

## 4. Teach useful behavior before asking self-play to discover everything

The local fixed-route experiment supplies a narrow but reproducible result:
BC then PPO scores 4016/4023/4009 of 4096 across three seeds, while scratch PPO
scores zero in all three. All 18 phase checkpoints were saved, reloaded and
reproduced. It uses one geometry and controls both coaches; it is not a
full-game neural-policy result.

Use that as motivation for a stronger next diagnostic: diverse, current-engine
teacher trajectories with held-out geometry, team side, roster, drive depth,
score and clock. Keep complete replay IDs together and BB2025 edition filtering
strict. The new BBP-v5 writer format is tested, but the replay mapper lacks handlers
for the restored Dump-off/Trickster windows. Repair and qualify that adapter
from actual raw BB2025 fixtures before producing new BC shards.
Human replay prefixes do not supply sufficient late-drive, comeback
or second-half behavior. Add authored legal trajectories and search teachers
for those gaps, with explicit teacher weaknesses and mixture weights.

Measure whether the main recurrent policy can acquire and retain these skills
through saved checkpoints. Report failures by decision family, including newly
restored optional choices. A useful curriculum must transfer to kickoff games
and held-out opponents; success on authored starts alone is insufficient.

## 5. Test search and opponent diversity as separate improvements

The latest accepted current-source test is D321, after both the Blitz–Stab
context repair and the inactive procedure-storage seed-key repair. Its full
frozen rig matrix completed 1,280 games with zero played/counterfactual errors.
In 512 paired contact cases, match utility improves by 3.515625 percentage
points; the 95% paired-seed bootstrap interval is [+1.2695, +5.6641] points.
Losses also increase by six games, so this is not improvement on every outcome.
In 128 paired offense cases, utility improves by 2.734375 points, with interval
[0, +5.46875]; broader transfer remains uncertain. Mean root search time was
29.89 ms for contact and 23.42 ms for offense on the local rig, with concurrent
diagnostic load. Earlier Mac and pre-repair measurements remain historical,
source-specific evidence and must not replace this result.

Search is a practical candidate for local play and a teacher for the neural
policy. Test a neural proposal/search combination against learned opponents,
with saved games and tail latency, before claiming neural improvement. Do not
use live future RNG values in counterfactual planning.

The existing opponent-composition proposal keeps scripted/historical exposure
fixed and changes only historical-policy diversity. Its old obs-v6 manifests
are immutable and obsolete for launching v7. After the prerequisites, freeze
new concrete identities and compare 100M steps per arm at two training seeds,
reversing run order. Its proposed initial exam of 32 games ×8 cells per model
is a large-failure screen, not a promotion test. Learned-opponent, roster,
longer-horizon and second-ancestry confirmation remain later stages.

## 6. Make evaluation reliable and affordable

Use frozen-policy evaluation with no optimizer updates, kickoff starts,
`demo_reset_pct=0`, both orientations, common-seed paired results, W/D/L,
TD for/against, complete-game counts and exact-zero integrity counters.
Record a named baseline bridge after each semantic change. Do not compare
historical training curves numerically across changed rules or reward meanings.

Implement lossless batched per-environment completion records before a large
exam. Current measured serial evaluation is about 2 seconds/game: 1,024 games
would take roughly 34 minutes and 32,000 games roughly 17.8 hours. Those estimates
apply to that measured serial path, not a claimed future batched speed.
Measure the actual new H512/L3/eight-bank training and evaluation configuration;
one-layer generic qualification throughput is not a production cost model.

## When new Vast spend becomes justified

Require all of the following before a paid run is proposed:

- A qualified immutable runtime and an explicit audited rule scope.
- A reproducible current-runtime baseline with complete saved match records.
- A local learning or held-out transfer signal that merits scaling.
- One declared experimental factor, complete reward manifests and fixed
  checkpoint/pool/seed identities; no adaptation inside the frozen run.
- Measured useful steps/second and games/second for the intended configuration.
- A capped step count, runtime, GPU spend, disk/thermal guards and artifact gates.

Compute the request from those measurements: training time = planned steps /
measured useful steps per second; evaluation time = planned games / measured
games per second. Provision only the resulting bounded job after the user funds
it. A failed integrity gate stops the run; it is not averaged in as poor play.
No reward or checkpoint receives production status from a two-seed scripted
screen. Current Vast spend remains zero.

Reliable local human-versus-bot play additionally needs an input adapter,
complete played-match records, supported rules coverage and measured decision
latency. FUMBBL is a later integration and permission/conformance milestone;
no online account, match or automated-play action has been taken.

## Reroll design follow-through

The initial shared-reroll design and a separate primary-rule resolution are
preserved with the audit. The resolution assigns TTM landing Team rerolls to
the thrown player's Loner and excludes either player's Pro because activation
and test ownership differ. It confirms Steady Footing's initial roll is
compulsory, while eligible reroll choices follow the result; Charge! permits
Team rerolls during its free activations. Two less explicit conclusions remain
labeled textual deductions rather than worked FAQ examples. These are design
inputs for future failing regressions, not code already implemented.

The complete shared transaction needs more typed public roll context than the
current design exposes. Plan an explicit compatibility revision if that context
cannot fit the existing documented fields; never overwrite a nested test's
player identity or expose overloaded private state as an observation shortcut.
Do not rebuild the currently frozen qualification artifact in place.

## What the completed baseline adds

The corrected-runtime migrated Chain9 scores 52.34% normalized match utility
over 256 games against two scripts. Cage home/away cells differ by 19.53 points
(64.84% versus 45.31%, 64 games each). Preserve this matrix and investigate
side/start/roster effects with prospective matched tests. Do not infer a new
encoding bug from this descriptive result alone.

Current-source script search improves contact match utility by 3.52 points
on 512 paired cases (interval +1.27 to +5.66). Its offense transfer interval
reaches zero. This supports a bounded search/teacher experiment; it does not
establish neural improvement or readiness for human competition. Keep the
rule-choice, recurrent-memory and diverse-teaching priorities above.
