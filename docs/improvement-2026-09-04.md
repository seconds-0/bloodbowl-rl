# September improvement program

The user authorized a sustained test–measure–repeat goal, Sol subagents, and
the local GPU. Paid Vast compute remains paused. This checkout is the
integration branch `codex/improvement-20260904`, based on campaign source
`141708fcb95e0296f22929a8328c46a67cb23a0b`. It preserves the user's original
checkouts and production viewer.

## Acceptance

The immediate goal is a reproducible learned capability or held-out match
strength improvement on a qualified stack. Correctness and tooling improvements
are necessary intermediate outcomes. A tactical drill result is not evidence of
championship strength. No production reward or checkpoint is promoted here.

1. Integrate minimal pass/handoff/restored-PBRS regressions and rollout-tail /
   entropy objective repairs; test engine, sanitizer, and patch applicability.
2. Build an isolated fp32 runtime on the RTX 2070. Qualify recurrence, masks,
   terminal rewards, graph-captured objective, and zero integrity counters.
3. Evaluate immutable chain 9 on the new runtime as a named bridge. Both sides,
   contact and cage scripts, explicit paired seeds, no optimizer updates.
4. Prove small-task learning, then confirm fixed exposure across fresh seeds.
5. Run an opponent-composition screen holding script and historical exposure
   fixed, with reward unchanged. Use two 61-row scripted seats and six 61-row
   learned seats in each 1,024-row buffer. Control duplicates three historical
   policies; candidate uses six policies. Do not compare the new arm causally
   against old campaign curves. Freeze concrete pool/plan hashes before launch.
6. Assess a bounded tactical-search teacher as a separate local-play avenue.

Each experiment must record source, installed module, complete effective config,
checkpoint and plan identity, planned exposure, actual exposure, explicit
training/evaluation phases, and zero invalid/non-finite/engine/fallback/clip
counters. Freeze each executable plan before observing its results. An integrity
failure stops that run; it is never averaged in as a losing result.

## September 4 model-facing audit amendment

The earlier numbered experiment order is superseded where it conflicts with
new harness evidence. All 108 generated skill/trait IDs now have a source/rules
review entry, and substantial common reroll, choice and mechanic gaps remain.
The integrated obs-v7 repair has 2,851 inputs; the earlier obs-v6 pools and
plans remain historical and cannot be launched against it. The new runtime
qualification uses separately migrated, ineligible evaluation checkpoints.

Immediate order: finish exact compiled-runtime qualification; preserve a named
checkpoint/engine bridge; implement and test the shared decision mechanics
needed by a declared training scope; then freeze the next bounded learning
comparison. Full-roster BB2025 claims require full conformance coverage.
The detailed repair backlog is
`audit-artifacts/model-facing-audit-20260904/integration-v7/bb-harness-repair-backlog-20260904.md`.
A rule repair or mathematically zero-extended checkpoint is not a production
promotion. Production viewer and historical sidecars remain untouched.

## Important correction to the initial audit

The old F5 pilot branch contains extensive protocol and tests, but its required
controller and verifier entrypoints are absent. The original dirty test and
untracked manifest remain untouched. The September diagnostic will have its
own name and evidence contract; it will not claim to satisfy that sealed pilot.
The initial audit's suggestion that the old pilot was nearly ready overstated
its executable readiness.

## Progress

- 2026-09-04: Created active goal and isolated integration/agent worktrees.
  Baseline `make test` passed on the integration source. Sol agents are porting
  repairs, building frozen evaluation, adding fixed-exposure script routing,
  preparing the isolated rig runtime, implementing a small-task diagnostic,
  and testing tactical search. The previous cleanup removed 49 of 77 worktrees
  after verifying source retention; unique and dirty work were preserved.

- 2026-09-04 14:18 PDT: Integrated native semantic fixes pass 592 engine /
  environment cases plus the writer, and matching sanitizer coverage. The
  pristine Puffer install is repeatable and checks overlapping patches exactly.
  Scoped native CUDA and Torch CPU rollout-tail verifiers pass. Recurrent CUDA
  qualification v2 passes construction, first-rollout graph-mode parity,
  terminal reset, frozen-row exclusion and unit PPO recomputation ratios. Its
  measured complete LR=0 rollout-plus-train rate is 138,339 steps/s on the RTX
  2070; this is not comparable to old rollout-only timing. Canonical copied
  evidence: `audit-artifacts/rig-stage-2/integrated-fp32-v2/QUALIFICATION.json`,
  SHA-256 `0ffb8fecd86ed4ab6102dac5cd0a9ab1f9b114db7fb5f61d57cd17b47633a451`.
  Native nonzero entropy graph behavior and full eight-bank group coverage
  remain separate pre-canary gates.
- 2026-09-04 14:18 PDT: The real CPU Torch train method passes three independent
  entropy-gradient / SGD-update oracle cells. Maximum update error is 1.14e-8
  against tolerance 2e-7. This uses a deterministic scalar-policy fixture with
  controlled advantages; it proves the entropy objective in that CPU method,
  not native CUDA parity or the complete F5 qualification. Evidence: 
  `audit-artifacts/entropy-cpu-new/EVIDENCE.json`.
- 2026-09-04 14:18 PDT: Root review rejected the original standalone eight-step
  curriculum result: tuple indices substituted value estimates for rewards
  during return construction, then immediate rewards for the final returns.
  Its zero-scoring outcome cannot support a claim about correctly implemented
  PPO. A regression and separately named corrected repeat are required. The
  independent BC-versus-scratch runner uses correct reward/return indices and
  remains under independent review and checkpoint-preserving replication.

- 2026-09-04 14:38 PDT: D292/D293 record bounded tactical-search gains against
  two scripted styles. Final contact test: 512 fresh paired cases, +3.81pp
  match utility, seed-cluster95% interval [+1.46,+6.15]pp. Offense transfer:
  128 fresh paired cases, +3.52pp, interval [+0.39,+6.64]pp. Both have zero
  played and counterfactual errors. The integration branch passes a separate
  functional bridge; a full fixed-matrix engine-source bridge is running.
- 2026-09-04 14:38 PDT: D294 corrects the standalone curriculum conclusion.
  Correct sparse returns produce final fixture scores3468/3225/90 of4096;
  reliability varies strongly. D295 records exact reproduction of BC+PPO
  scores4016/4023/4009 of4096 versus scratch0 for each seed, with18 saved,
  reloaded checkpoints and matching parameter digests. Both diagnostics
  control either decision side after turnovers and test one fixed geometry.
  Neither demonstrates neural-champion or full-match improvement.
- 2026-09-04 14:38 PDT: The eight-bank snapshot problem was corrected at design
  review: its full fp32 rollout payload is about1.70GB, so raising a cap to
  1GiB would still fail and would waste host copies. A compact read-only
  snapshot retains the needed ratios, selected rows and decoder groups under
  the existing64MiB bound. Read-only entropy gradients and actual graph counters
  were added as separate patches. A single isolated rebuild and new module-
  bound qualification set are now queued; old failures remain preserved.
- 2026-09-04 14:38 PDT: The pool comparison's initial exam budget was amended
  prospectively, before any training outcome, to32 complete games per cell
  across8 cells/model, final100M checkpoints only (1,024 trained-policy games).
  This is an exploratory large-failure/asymmetry screen. The original1000/cell
  scripted grid would take about17.8h with the measured serial runner; a
  lossless batched-evaluation design now specifies the missing per-game native
  completion records. Larger learned-opponent/roster confirmation stays separate.
