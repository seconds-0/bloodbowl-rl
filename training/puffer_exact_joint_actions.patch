diff --git a/pufferlib/torch_pufferl.py b/pufferlib/torch_pufferl.py
index f4d45d978ee2eff897a83f54e3b1fd58b73c5dcd..dab7fb638e435d89ba1ff90f9ad60c6cfd8678c1 100644
--- a/pufferlib/torch_pufferl.py
+++ b/pufferlib/torch_pufferl.py
@@ -48,2 +48,77 @@ def _entropy(logits):
 
+def apply_action_mask(logits, mask, act_sizes):
+    """Restrict each categorical head to the supplied support slice."""
+    out = []
+    off = 0
+    for h, n in enumerate(act_sizes):
+        mh = mask[:, off:off + n].bool()
+        off += n
+        if not bool(mh.any(dim=-1).all().item()):
+            raise RuntimeError('action mask row has empty head support')
+        out.append(logits[h].masked_fill(~mh, float('-inf')))
+    return out
+
+def compact_joint_actions(packed, offsets, counts):
+    """Gather ragged row segments into physical-row order on the host.
+
+    Native vec packing keeps each buffer in a fixed-capacity region and may
+    visit physical rows out of order after a self-play permutation. Offsets
+    are therefore authoritative; slicing a global prefix is not.
+    """
+    counts_long = counts.long()
+    offsets_long = offsets.long()
+    if counts_long.ndim != 1 or offsets_long.shape != counts_long.shape:
+        raise RuntimeError('joint action offsets/counts must be matching vectors')
+    if not bool((counts_long > 0).all().item()):
+        raise RuntimeError('every joint action row must have nonempty support')
+    compact_offsets = torch.cumsum(counts_long, dim=0) - counts_long
+    total = int(counts_long.sum().item())
+    rows = torch.repeat_interleave(
+        torch.arange(counts_long.numel(), device=counts.device), counts_long)
+    within = torch.arange(total, device=counts.device) - compact_offsets[rows]
+    source = offsets_long[rows] + within
+    if source.numel() != total or int(source.min().item()) < 0 or \
+            int(source.max().item()) >= packed.numel():
+        raise RuntimeError('joint action offset/count escapes packed capacity')
+    return packed[source], compact_offsets.int()
+
+def sample_joint_logits(logits, packed, offsets, counts, act_sizes):
+    """Sample semantic heads from ragged exact joint support.
+
+    Returns the selected conditional masks so ordinary masked PPO
+    recomputation scores the exact behavior distribution without retaining
+    ragged support across the rollout horizon.
+    """
+    batch = logits[0].shape[0]
+    device = logits[0].device
+    rows = torch.repeat_interleave(
+        torch.arange(batch, device=device), counts.long())
+    if rows.numel() != packed.numel():
+        raise RuntimeError('joint action offsets/counts do not match packed support')
+    expected = torch.cumsum(counts.long(), dim=0) - counts.long()
+    if not torch.equal(offsets.long(), expected):
+        raise RuntimeError('torch joint support must be globally compact')
+
+    prefix = torch.ones(packed.shape[0], dtype=torch.bool, device=device)
+    actions, logprobs, entropies, masks = [], [], [], []
+    for h, size in enumerate(act_sizes):
+        values = (packed.long() >> (10 * h)) & 1023
+        support = torch.zeros((batch, size), dtype=torch.bool, device=device)
+        support[rows[prefix], values[prefix]] = True
+        if not bool(support.any(dim=1).all().item()):
+            raise RuntimeError(f'empty exact joint support at action head {h}')
+        masked = logits[h].masked_fill(~support, float('-inf'))
+        action, logprob, entropy = sample_logits(masked)
+        selected = action.reshape(batch).long()
+        actions.append(selected)
+        logprobs.append(logprob)
+        entropies.append(entropy)
+        masks.append(support)
+        prefix &= values == selected[rows]
+
+    return (torch.stack(actions, dim=1).int(),
+            torch.stack(logprobs, dim=0).sum(dim=0),
+            torch.stack(entropies, dim=0).sum(dim=0),
+            torch.cat(masks, dim=1).to(torch.uint8))
+
 def sample_logits(logits, action=None):
@@ -99,2 +174,3 @@ _TORCH_TO_CTYPE = {
     torch.float32: ctypes.c_float,
+    torch.int32:   ctypes.c_int32,
 }
@@ -146,2 +222,23 @@ class PuffeRL:
 
+        self.mask_size = int(getattr(vec, 'action_mask_size', 0) or 0)
+        self.act_sizes_list = list(vec.act_sizes)
+        mask_ptr = int(getattr(vec, 'action_mask_ptr', 0) or 0)
+        if self.mask_size > 0 and mask_ptr != 0:
+            self.vec_action_mask = _cpu_tensor(
+                mask_ptr, (total_agents, self.mask_size), torch.uint8)
+        else:
+            self.vec_action_mask = None
+        self.joint_action_capacity = int(
+            getattr(vec, 'joint_action_capacity', 0) or 0)
+        joint_ptr = int(getattr(vec, 'joint_actions_ptr', 0) or 0)
+        if self.joint_action_capacity > 0 and joint_ptr != 0:
+            self.vec_joint_actions = _cpu_tensor(
+                joint_ptr, (self.joint_action_capacity,), torch.int32)
+            self.vec_joint_offsets = _cpu_tensor(
+                vec.joint_action_offsets_ptr, (total_agents,), torch.int32)
+            self.vec_joint_counts = _cpu_tensor(
+                vec.joint_action_counts_ptr, (total_agents,), torch.int32)
+        else:
+            self.vec_joint_actions = None
+
         vec.reset()
@@ -157,2 +254,6 @@ class PuffeRL:
         self.terminals = torch.zeros(horizon, total_agents, device=device)
+        self.action_masks = (torch.zeros(
+            horizon, total_agents, self.mask_size,
+            dtype=torch.uint8, device=device)
+            if self.mask_size > 0 else None)
         self.ratio = torch.ones(total_agents, horizon, device=device)
@@ -221,3 +322,22 @@ class PuffeRL:
                 logits, value, state = self.policy.forward_eval(o_device, self.state)
-                action, logprob, _ = sample_logits(logits)
+                if self.vec_joint_actions is not None:
+                    counts_cpu = self.vec_joint_counts.clone()
+                    source_offsets_cpu = self.vec_joint_offsets.clone()
+                    packed_cpu, offsets_cpu = compact_joint_actions(
+                        self.vec_joint_actions, source_offsets_cpu,
+                        counts_cpu)
+                    packed = packed_cpu.to(device=device)
+                    offsets = offsets_cpu.to(device=device)
+                    counts = counts_cpu.to(device=device)
+                    action, logprob, _, effective_mask = sample_joint_logits(
+                        logits, packed, offsets, counts, self.act_sizes_list)
+                elif self.vec_action_mask is not None:
+                    effective_mask = torch.as_tensor(
+                        self.vec_action_mask, device=device).clone()
+                    logits = apply_action_mask(
+                        logits, effective_mask, self.act_sizes_list)
+                    action, logprob, _ = sample_logits(logits)
+                else:
+                    effective_mask = None
+                    action, logprob, _ = sample_logits(logits)
             prof.mark(2)
@@ -232,2 +352,6 @@ class PuffeRL:
                 self.values[t] = value.flatten()
+                if self.action_masks is not None:
+                    if effective_mask is None:
+                        raise RuntimeError('action-mask rollout buffer has no mask')
+                    self.action_masks[t] = effective_mask
 
@@ -279,2 +403,4 @@ class PuffeRL:
         ter = self.terminals.T.contiguous()
+        masks = (self.action_masks.transpose(0, 1).contiguous()
+                 if self.action_masks is not None else None)
 
@@ -303,2 +429,3 @@ class PuffeRL:
             mb_advantages = advantages[idx]
+            mb_masks = masks[idx] if masks is not None else None
 
@@ -306,3 +433,9 @@ class PuffeRL:
             logits, newvalue = self.policy(mb_obs)
-            actions, newlogprob, entropy = sample_logits(logits, action=mb_actions)
+            masked_logits = logits
+            if mb_masks is not None:
+                masked_logits = apply_action_mask(
+                    logits, mb_masks.reshape(-1, self.mask_size),
+                    self.act_sizes_list)
+            actions, newlogprob, entropy = sample_logits(
+                masked_logits, action=mb_actions)
             prof.mark(2)
diff --git a/src/bindings.cu b/src/bindings.cu
index f3f3d01da86d04244960aa3f629775f627362f29..1dc6d92b4e8d0f66a0854bf26290b7d653d5586b 100644
--- a/src/bindings.cu
+++ b/src/bindings.cu
@@ -6,2 +6,3 @@
 #include "pufferlib.cu"
+#include "exact_action_build_hash.h"
 
@@ -511,2 +512,7 @@ PYBIND11_MODULE(_C, m) {
     m.attr("gpu") = 1;
+    m.attr("exact_action_source_hash") = PUFFER_EXACT_ACTION_SOURCE_HASH;
+    m.attr("environment_source_hash") = PUFFER_ENV_SOURCE_HASH;
+    m.attr("observation_abi") = PUFFER_OBSERVATION_ABI;
+    m.attr("observation_version") = PUFFER_OBSERVATION_VERSION;
+    m.attr("action_abi") = PUFFER_ACTION_ABI;
 
@@ -608,2 +614,3 @@ PYBIND11_MODULE(_C, m) {
         .def_property_readonly("gpu_terminals_ptr", [](VecEnv& ve) { return (long long)ve.vec->gpu_terminals; })
+        .def_property_readonly("gpu_action_mask_ptr", [](VecEnv& ve) { return (long long)ve.vec->gpu_action_mask; })
         // CPU buffer pointers (same as gpu_ in CPU mode since they alias)
@@ -612,2 +619,16 @@ PYBIND11_MODULE(_C, m) {
         .def_property_readonly("terminals_ptr", [](VecEnv& ve) { return (long long)ve.vec->terminals; })
+        .def_property_readonly("action_mask_ptr", [](VecEnv& ve) { return (long long)ve.vec->action_mask; })
+        .def_property_readonly("action_mask_size", [](VecEnv& ve) { return ve.vec->action_mask_size; })
+        .def_property_readonly("joint_actions_ptr", [](VecEnv& ve) { return (long long)ve.vec->joint_actions; })
+        .def_property_readonly("joint_action_offsets_ptr", [](VecEnv& ve) { return (long long)ve.vec->joint_action_offsets; })
+        .def_property_readonly("joint_action_counts_ptr", [](VecEnv& ve) { return (long long)ve.vec->joint_action_counts; })
+        .def_property_readonly("joint_action_capacity", [](VecEnv& ve) { return ve.vec->joint_action_capacity; })
+        .def_property_readonly("joint_action_count", [](VecEnv& ve) {
+            int total = 0;
+            for (int b = 0; b < ve.vec->buffers; b++) {
+                total += ve.vec->joint_buffer_counts
+                    ? ve.vec->joint_buffer_counts[b] : 0;
+            }
+            return total;
+        })
         .def("reset", &vec_reset)
diff --git a/src/bindings_cpu.cpp b/src/bindings_cpu.cpp
index 22caa5e9bd09fc18d237b69de5cb4ff3182e494c..e7215b603372cd6cfe6f4108d6aaa363382362ae 100644
--- a/src/bindings_cpu.cpp
+++ b/src/bindings_cpu.cpp
@@ -11,2 +11,3 @@
 #include "vecenv.h"
+#include "exact_action_build_hash.h"
 
@@ -166,2 +167,7 @@ PYBIND11_MODULE(_C, m) {
     m.attr("gpu") = 0;
+    m.attr("exact_action_source_hash") = PUFFER_EXACT_ACTION_SOURCE_HASH;
+    m.attr("environment_source_hash") = PUFFER_ENV_SOURCE_HASH;
+    m.attr("observation_abi") = PUFFER_OBSERVATION_ABI;
+    m.attr("observation_version") = PUFFER_OBSERVATION_VERSION;
+    m.attr("action_abi") = PUFFER_ACTION_ABI;
 
@@ -181,2 +187,16 @@ PYBIND11_MODULE(_C, m) {
         .def_property_readonly("terminals_ptr", [](VecEnv& ve) { return (long long)ve.vec->terminals; })
+        .def_property_readonly("action_mask_ptr", [](VecEnv& ve) { return (long long)ve.vec->action_mask; })
+        .def_property_readonly("action_mask_size", [](VecEnv& ve) { return ve.vec->action_mask_size; })
+        .def_property_readonly("joint_actions_ptr", [](VecEnv& ve) { return (long long)ve.vec->joint_actions; })
+        .def_property_readonly("joint_action_offsets_ptr", [](VecEnv& ve) { return (long long)ve.vec->joint_action_offsets; })
+        .def_property_readonly("joint_action_counts_ptr", [](VecEnv& ve) { return (long long)ve.vec->joint_action_counts; })
+        .def_property_readonly("joint_action_capacity", [](VecEnv& ve) { return ve.vec->joint_action_capacity; })
+        .def_property_readonly("joint_action_count", [](VecEnv& ve) {
+            int total = 0;
+            for (int b = 0; b < ve.vec->buffers; b++) {
+                total += ve.vec->joint_buffer_counts
+                    ? ve.vec->joint_buffer_counts[b] : 0;
+            }
+            return total;
+        })
         .def("reset", &vec_reset)
diff --git a/src/kernels.cu b/src/kernels.cu
index 0d878f01f451ae0fe932469c10baa00ac307cc7d..bb7858b34498f55f3bd9342293a5932782e91a40 100644
--- a/src/kernels.cu
+++ b/src/kernels.cu
@@ -322,2 +322,5 @@ inline void cast_dispatch(precision_t* dst, const precision_t* src, int n, cudaS
 }
+// Under PRECISION_FLOAT, precision_t == float and this overload would collide
+// with the device-to-device copy overload above.
+#ifndef PRECISION_FLOAT
 inline void cast_dispatch(precision_t* dst, const float* src, int n, cudaStream_t stream) {
@@ -325,2 +328,3 @@ inline void cast_dispatch(precision_t* dst, const float* src, int n, cudaStream_
 }
+#endif
 
diff --git a/src/pufferlib.cu b/src/pufferlib.cu
index fb98fc14be33a90c7e9d59dce6d8bbae0a42cdf7..218d830fbc3b98e3dee2395e68320d1619d1d97e 100644
--- a/src/pufferlib.cu
+++ b/src/pufferlib.cu
@@ -238,2 +238,5 @@ struct EnvBuf {
     ByteTensor action_mask; // (total_agents, mask_size); .data=nullptr when env opts out
+    IntTensor joint_actions; // packed ragged support; empty for ordinary envs
+    IntTensor joint_action_offsets; // (total_agents,)
+    IntTensor joint_action_counts;  // (total_agents,)
 };
@@ -243,2 +246,18 @@ StaticVec* create_environments(int num_buffers, int total_agents,
     StaticVec* vec = create_static_vec(total_agents, num_buffers, 1, vec_kwargs, env_kwargs);
+    if (vec->joint_action_max > 0) {
+        if (vec->action_mask_size <= 0 || get_num_act_sizes() > 3) {
+            fprintf(stderr,
+                "joint actions require an action mask and at most three heads\n");
+            abort();
+        }
+        int* sizes = get_act_sizes();
+        for (int h = 0; h < get_num_act_sizes(); h++) {
+            if (sizes[h] <= 0 || sizes[h] > 1023) {
+                fprintf(stderr,
+                    "joint action head %d size %d exceeds 10-bit packing\n",
+                    h, sizes[h]);
+                abort();
+            }
+        }
+    }
     env.obs = {
@@ -256,2 +275,20 @@ StaticVec* create_environments(int num_buffers, int total_agents,
     }
+    if (vec->joint_action_max > 0) {
+        env.joint_actions = {
+            .data = (int*)vec->gpu_joint_actions,
+            .shape = {vec->joint_action_capacity},
+        };
+        env.joint_action_offsets = {
+            .data = vec->gpu_joint_action_offsets,
+            .shape = {total_agents},
+        };
+        env.joint_action_counts = {
+            .data = vec->gpu_joint_action_counts,
+            .shape = {total_agents},
+        };
+    } else {
+        env.joint_actions = {.data = nullptr, .shape = {0}};
+        env.joint_action_offsets = {.data = nullptr, .shape = {0}};
+        env.joint_action_counts = {.data = nullptr, .shape = {0}};
+    }
     return vec;
@@ -430,11 +467,7 @@ __device__ __forceinline__ float safe_logit(const precision_t* logits,
 
-__device__ __forceinline__ float masked_logit(const precision_t* logits,
-        int logits_base, int logits_offset, int offset,
-        const precision_t* mask, int mask_base) {
-    float l = safe_logit(logits, logits_base, logits_offset, offset);
-    if (mask != nullptr) {
-        float m = to_float(mask[mask_base + logits_offset + offset]);
-        if (m == 0.0f) l = -1e4f;
-    }
-    return l;
+__device__ __forceinline__ bool action_enabled(
+        const precision_t* mask, int mask_base,
+        int logits_offset, int offset) {
+    return mask == nullptr ||
+        to_float(mask[mask_base + logits_offset + offset]) != 0.0f;
 }
@@ -450,4 +483,7 @@ __global__ void sample_logits(
         curandStatePhilox4_32_10_t* __restrict__ rng_states,
-        const precision_t* __restrict__ action_mask, // (B, A_total) or nullptr
-        int mask_stride) {                    // 0 when action_mask is nullptr
+        precision_t* __restrict__ action_mask, // (B, A_total) or nullptr
+        int mask_stride,                       // 0 when action_mask is nullptr
+        const int* __restrict__ joint_actions, // packed 10-bit head values
+        const int* __restrict__ joint_offsets, // (B,), global packed offsets
+        const int* __restrict__ joint_counts) { // (B,)
     int B = dec_out.shape[0];
@@ -504,2 +540,34 @@ __global__ void sample_logits(
 
+            // Exact sequential support. Rewrite the rollout mask row with the
+            // support conditioned on heads sampled earlier in this same
+            // transition. That mask is then both the behavior distribution
+            // and the complete artifact PPO needs for recomputation.
+            if (joint_actions != nullptr) {
+                for (int a = 0; a < A; ++a) {
+                    action_mask[mask_base + logits_offset + a] =
+                        from_float(0.0f);
+                }
+                int off = joint_offsets[idx];
+                int count = joint_counts[idx];
+                for (int j = 0; j < count; ++j) {
+                    unsigned int packed = (unsigned int)joint_actions[off + j];
+                    bool prefix = true;
+                    for (int p = 0; p < h; ++p) {
+                        int expected = (packed >> (10 * p)) & 1023;
+                        int selected = (int)to_float(
+                            actions[idx * num_atns + p]);
+                        if (expected != selected) {
+                            prefix = false;
+                            break;
+                        }
+                    }
+                    if (!prefix) continue;
+                    int value = (packed >> (10 * h)) & 1023;
+                    if (value >= 0 && value < A) {
+                        action_mask[mask_base + logits_offset + value] =
+                            from_float(1.0f);
+                    }
+                }
+            }
+
             // Step 1: Find max and sum for numerical stability (with nan_to_num)
@@ -507,4 +575,8 @@ __global__ void sample_logits(
             float sum_exp = 0.0f;
+            int enabled_count = 0;
             for (int a = 0; a < A; ++a) {
-                float l = masked_logit(logits, logits_base, logits_offset, a, action_mask, mask_base);
+                if (!action_enabled(action_mask, mask_base, logits_offset, a)) {
+                    continue;
+                }
+                float l = safe_logit(logits, logits_base, logits_offset, a);
                 if (l > max_val) {
@@ -514,2 +586,10 @@ __global__ void sample_logits(
                 sum_exp += expf(l - max_val);
+                enabled_count += 1;
+            }
+            assert(enabled_count > 0);
+            if (enabled_count == 0) {
+                actions[idx * num_atns + h] = from_float(0.0f);
+                total_log_prob = NAN;
+                logits_offset += A;
+                continue;
             }
@@ -525,3 +605,6 @@ __global__ void sample_logits(
             for (int a = 0; a < A; ++a) {
-                float l = masked_logit(logits, logits_base, logits_offset, a, action_mask, mask_base);
+                if (!action_enabled(action_mask, mask_base, logits_offset, a)) {
+                    continue;
+                }
+                float l = safe_logit(logits, logits_base, logits_offset, a);
                 float prob = expf(l - logsumexp);
@@ -537,8 +620,6 @@ __global__ void sample_logits(
                 sampled_action = A - 1;
-                if (action_mask != nullptr) {
-                    for (int a = A - 1; a >= 0; --a) {
-                        if (to_float(action_mask[mask_base + logits_offset + a]) != 0.0f) {
-                            sampled_action = a;
-                            break;
-                        }
+                for (int a = A - 1; a >= 0; --a) {
+                    if (action_enabled(action_mask, mask_base, logits_offset, a)) {
+                        sampled_action = a;
+                        break;
                     }
@@ -548,3 +629,5 @@ __global__ void sample_logits(
             // Step 5: Gather log probability of sampled action
-            float sampled_logit = masked_logit(logits, logits_base, logits_offset, sampled_action, action_mask, mask_base);
+            assert(action_enabled(action_mask, mask_base, logits_offset, sampled_action));
+            float sampled_logit = safe_logit(
+                logits, logits_base, logits_offset, sampled_action);
             float log_prob = sampled_logit - logsumexp;
@@ -681,3 +764,8 @@ extern "C" void net_callback_wrapper(void* ctx, int buf, int t) {
             pufferl->rng_states[buf] + bank_off,
-            mask_b.data, mask_stride_b);
+            mask_b.data, mask_stride_b,
+            env.joint_actions.data,
+            env.joint_action_offsets.data == nullptr
+                ? nullptr : env.joint_action_offsets.data + sub_start,
+            env.joint_action_counts.data == nullptr
+                ? nullptr : env.joint_action_counts.data + sub_start);
 
@@ -701,15 +789,7 @@ extern "C" void net_callback_wrapper(void* ctx, int buf, int t) {
 
-__device__ __forceinline__ float load_logit_masked(
+__device__ __forceinline__ float load_logit(
         const precision_t* __restrict__ logits, int logits_base,
-        int logits_stride_a, int logits_offset, int a,
-        const precision_t* __restrict__ mask, int mask_base) {
-    float l = to_float(logits[logits_base + (logits_offset + a) * logits_stride_a]);
-    if (mask != nullptr) {
-        float m = to_float(mask[mask_base + logits_offset + a]);
-        if (m == 0.0f) {
-            l = -1e4f;
-            return l;
-        }
-    }
-    return l;
+        int logits_stride_a, int logits_offset, int a) {
+    return to_float(
+        logits[logits_base + (logits_offset + a) * logits_stride_a]);
 }
@@ -724,5 +804,10 @@ __device__ __forceinline__ void ppo_discrete_head(
     float act_logit = 0.0f;
+    int enabled_count = 0;
 
     for (int a = 0; a < A; ++a) {
-        float l = load_logit_masked(logits, logits_base, logits_stride_a, logits_offset, a, mask, mask_base);
+        if (!action_enabled(mask, mask_base, logits_offset, a)) {
+            continue;
+        }
+        float l = load_logit(
+            logits, logits_base, logits_stride_a, logits_offset, a);
         if (a == act) {
@@ -735,2 +820,13 @@ __device__ __forceinline__ void ppo_discrete_head(
         sum += __expf(l - max_logit);
+        enabled_count += 1;
+    }
+    bool act_enabled = act >= 0 && act < A &&
+        action_enabled(mask, mask_base, logits_offset, act);
+    assert(enabled_count > 0);
+    assert(act_enabled);
+    if (enabled_count == 0 || !act_enabled) {
+        *out_logsumexp = 0.0f;
+        *out_entropy = 0.0f;
+        *out_logp = -INFINITY;
+        return;
     }
@@ -740,3 +836,7 @@ __device__ __forceinline__ void ppo_discrete_head(
     for (int a = 0; a < A; ++a) {
-        float l = load_logit_masked(logits, logits_base, logits_stride_a, logits_offset, a, mask, mask_base);
+        if (!action_enabled(mask, mask_base, logits_offset, a)) {
+            continue;
+        }
+        float l = load_logit(
+            logits, logits_base, logits_stride_a, logits_offset, a);
         float logp = l - logsumexp;
@@ -894,4 +994,9 @@ __global__ void ppo_loss_compute(
             for (int j = 0; j < A; ++j) {
-                float l = load_logit_masked(a.logits, logits_base, a.logits_stride_a,
-                                            logits_offset, j, a.action_mask, mask_base);
+                if (!action_enabled(
+                        a.action_mask, mask_base, logits_offset, j)) {
+                    a.grad_logits[grad_logits_base + logits_offset + j] = 0.0f;
+                    continue;
+                }
+                float l = load_logit(a.logits, logits_base, a.logits_stride_a,
+                                     logits_offset, j);
                 float logp = l - logsumexp;
@@ -1870,2 +1975,7 @@ extern "C" void pufferl_load_frozen_bank(PuffeRL* pufferl, int bank_idx, const c
 extern "C" void pufferl_set_agent_perm(PuffeRL* pufferl, const int* perm) {
+    if (pufferl->global_step != 0 || pufferl->vec->agent_perm != NULL) {
+        fprintf(stderr,
+            "pufferl_set_agent_perm: permutation is a one-time pre-rollout operation\n");
+        abort();
+    }
     int total = pufferl->vec->total_agents;
@@ -2100,2 +2210,7 @@ std::unique_ptr<PuffeRL> create_pufferl_impl(HypersT& hypers,
 
+    // Exact-action environments allocate ragged joint metadata outside the
+    // activations allocator. Populate and upload it before graph warmup calls
+    // sample_logits; cudaMalloc storage is otherwise uninitialized here.
+    if (vec->joint_action_max > 0) static_vec_reset(vec);
+
     // Cudagraph rolluts and entire training step
@@ -2181,4 +2296,5 @@ std::unique_ptr<PuffeRL> create_pufferl_impl(HypersT& hypers,
     create_static_threads(vec, hypers.num_threads, horizon, pufferl.get(),
-        net_callback_wrapper, thread_init_wrapper);
-    static_vec_reset(vec);
+        net_callback_wrapper,
+        thread_init_wrapper);
+    if (vec->joint_action_max <= 0) static_vec_reset(vec);
 
diff --git a/src/vecenv.h b/src/vecenv.h
index b920161dbcf804b244b3e85b98b56cdf6da6059b..3c112ef5ba9de623fcfa465956318a0b98e0b033 100644
--- a/src/vecenv.h
+++ b/src/vecenv.h
@@ -10,2 +10,3 @@
 #include <assert.h>
+#include <stdint.h>
 
@@ -100,2 +101,14 @@ typedef struct StaticVec {
     unsigned char* gpu_action_mask;  // NULL unless env defines MY_ACTION_MASK
+    // Optional ragged joint action support. Each uint32 packs up to three
+    // 10-bit semantic head values. Offsets/counts are indexed by agent row;
+    // actions are compacted independently inside each environment buffer.
+    uint32_t* joint_actions;
+    uint32_t* gpu_joint_actions;
+    int* joint_action_offsets;
+    int* gpu_joint_action_offsets;
+    int* joint_action_counts;
+    int* gpu_joint_action_counts;
+    int* joint_buffer_counts;
+    int joint_action_capacity;
+    int joint_action_max;
     cudaStream_t* streams;
@@ -221,2 +234,7 @@ void my_log(Log* log, Dict* out);
 
+#ifdef MY_JOINT_ACTION_MAX
+void my_pack_joint_actions(StaticVec* vec, Env* envs, int env_start,
+                           int env_count, int buf);
+#endif
+
 #ifdef MY_USES_PERM
@@ -304,2 +322,5 @@ static void* static_omp_threadmanager(void* arg) {
             }
+#ifdef MY_JOINT_ACTION_MAX
+            my_pack_joint_actions(vec, envs, env_start, env_count, buf);
+#endif
             clock_gettime(CLOCK_MONOTONIC, &t1);
@@ -328,2 +349,18 @@ static void* static_omp_threadmanager(void* arg) {
                 cudaMemcpyHostToDevice, stream);
+#endif
+#ifdef MY_JOINT_ACTION_MAX
+            int joint_start = agent_start * MY_JOINT_ACTION_MAX;
+            cudaMemcpyAsync(
+                vec->gpu_joint_action_offsets + agent_start,
+                vec->joint_action_offsets + agent_start,
+                agents_per_buffer * sizeof(int), cudaMemcpyHostToDevice, stream);
+            cudaMemcpyAsync(
+                vec->gpu_joint_action_counts + agent_start,
+                vec->joint_action_counts + agent_start,
+                agents_per_buffer * sizeof(int), cudaMemcpyHostToDevice, stream);
+            cudaMemcpyAsync(
+                vec->gpu_joint_actions + joint_start,
+                vec->joint_actions + joint_start,
+                (size_t)vec->joint_buffer_counts[buf] * sizeof(uint32_t),
+                cudaMemcpyHostToDevice, stream);
 #endif
@@ -468,2 +505,30 @@ StaticVec* create_static_vec(int total_agents, int num_buffers, int gpu, Dict* v
     }
+#endif
+#ifdef MY_JOINT_ACTION_MAX
+    vec->joint_action_max = MY_JOINT_ACTION_MAX;
+    vec->joint_action_capacity = total_agents * MY_JOINT_ACTION_MAX;
+    size_t joint_bytes =
+        (size_t)vec->joint_action_capacity * sizeof(uint32_t);
+    size_t joint_index_bytes = (size_t)total_agents * sizeof(int);
+    vec->joint_buffer_counts = (int*)calloc(num_buffers, sizeof(int));
+    if (gpu) {
+        cudaHostAlloc((void**)&vec->joint_actions, joint_bytes,
+                      cudaHostAllocPortable);
+        cudaHostAlloc((void**)&vec->joint_action_offsets, joint_index_bytes,
+                      cudaHostAllocPortable);
+        cudaHostAlloc((void**)&vec->joint_action_counts, joint_index_bytes,
+                      cudaHostAllocPortable);
+        cudaMalloc((void**)&vec->gpu_joint_actions, joint_bytes);
+        cudaMalloc((void**)&vec->gpu_joint_action_offsets, joint_index_bytes);
+        cudaMalloc((void**)&vec->gpu_joint_action_counts, joint_index_bytes);
+    } else {
+        vec->joint_actions =
+            (uint32_t*)calloc((size_t)vec->joint_action_capacity,
+                              sizeof(uint32_t));
+        vec->joint_action_offsets = (int*)calloc(total_agents, sizeof(int));
+        vec->joint_action_counts = (int*)calloc(total_agents, sizeof(int));
+        vec->gpu_joint_actions = vec->joint_actions;
+        vec->gpu_joint_action_offsets = vec->joint_action_offsets;
+        vec->gpu_joint_action_counts = vec->joint_action_counts;
+    }
 #endif
@@ -529,2 +594,10 @@ void static_vec_set_perm(StaticVec* vec, const int* perm) {
     }
+
+    // my_setup_perm changes every env's output/action destinations, but the
+    // rows currently stored there still describe the old identity layout.
+    // Re-emit and upload a coherent initial state before the first rollout.
+    // Permutations are configuration-time operations (self-play setup and
+    // policy-vs-policy evaluation), so resetting here is both deterministic
+    // and safer than allowing one stale rollout through the sampler.
+    static_vec_reset(vec);
 #endif
@@ -578,2 +651,8 @@ void static_vec_reset(StaticVec* vec) {
     }
+#ifdef MY_JOINT_ACTION_MAX
+    for (int buf = 0; buf < vec->buffers; buf++) {
+        my_pack_joint_actions(vec, envs, vec->buffer_env_starts[buf],
+                              vec->buffer_env_counts[buf], buf);
+    }
+#endif
     if (vec->gpu) {
@@ -587,2 +666,18 @@ void static_vec_reset(StaticVec* vec) {
             cudaMemcpyHostToDevice);
+#endif
+#ifdef MY_JOINT_ACTION_MAX
+        cudaMemcpy(vec->gpu_joint_action_offsets, vec->joint_action_offsets,
+                   (size_t)vec->total_agents * sizeof(int),
+                   cudaMemcpyHostToDevice);
+        cudaMemcpy(vec->gpu_joint_action_counts, vec->joint_action_counts,
+                   (size_t)vec->total_agents * sizeof(int),
+                   cudaMemcpyHostToDevice);
+        for (int buf = 0; buf < vec->buffers; buf++) {
+            int joint_start =
+                buf * vec->agents_per_buffer * MY_JOINT_ACTION_MAX;
+            cudaMemcpy(vec->gpu_joint_actions + joint_start,
+                       vec->joint_actions + joint_start,
+                       (size_t)vec->joint_buffer_counts[buf] * sizeof(uint32_t),
+                       cudaMemcpyHostToDevice);
+        }
 #endif
@@ -658,2 +753,10 @@ void static_vec_close(StaticVec* vec) {
         cudaFreeHost(vec->action_mask);
+#endif
+#ifdef MY_JOINT_ACTION_MAX
+        cudaFree(vec->gpu_joint_actions);
+        cudaFree(vec->gpu_joint_action_offsets);
+        cudaFree(vec->gpu_joint_action_counts);
+        cudaFreeHost(vec->joint_actions);
+        cudaFreeHost(vec->joint_action_offsets);
+        cudaFreeHost(vec->joint_action_counts);
 #endif
@@ -666,2 +769,7 @@ void static_vec_close(StaticVec* vec) {
         free(vec->action_mask);
+#endif
+#ifdef MY_JOINT_ACTION_MAX
+        free(vec->joint_actions);
+        free(vec->joint_action_offsets);
+        free(vec->joint_action_counts);
 #endif
@@ -670,2 +778,5 @@ void static_vec_close(StaticVec* vec) {
     free(vec->streams);
+#ifdef MY_JOINT_ACTION_MAX
+    free(vec->joint_buffer_counts);
+#endif
     if (vec->agent_perm != NULL) free(vec->agent_perm);
@@ -758,2 +869,8 @@ static inline void _static_vec_env_step(StaticVec* vec) {
     }
+#ifdef MY_JOINT_ACTION_MAX
+    for (int buf = 0; buf < vec->buffers; buf++) {
+        my_pack_joint_actions(vec, envs, vec->buffer_env_starts[buf],
+                              vec->buffer_env_counts[buf], buf);
+    }
+#endif
 }
@@ -761,3 +878,6 @@ static inline void _static_vec_env_step(StaticVec* vec) {
 void gpu_vec_step(StaticVec* vec) {
-    assert(vec->buffers == 1);
+    if (vec->buffers != 1) {
+        fprintf(stderr, "gpu_vec_step: synchronous stepping requires one buffer\n");
+        abort();
+    }
     cudaMemcpy(vec->actions, vec->gpu_actions,
@@ -773,2 +893,13 @@ void gpu_vec_step(StaticVec* vec) {
         vec->total_agents * sizeof(float), cudaMemcpyHostToDevice);
+#ifdef MY_JOINT_ACTION_MAX
+    cudaMemcpy(vec->gpu_joint_action_offsets, vec->joint_action_offsets,
+               (size_t)vec->total_agents * sizeof(int),
+               cudaMemcpyHostToDevice);
+    cudaMemcpy(vec->gpu_joint_action_counts, vec->joint_action_counts,
+               (size_t)vec->total_agents * sizeof(int),
+               cudaMemcpyHostToDevice);
+    cudaMemcpy(vec->gpu_joint_actions, vec->joint_actions,
+               (size_t)vec->joint_buffer_counts[0] * sizeof(uint32_t),
+               cudaMemcpyHostToDevice);
+#endif
 }
@@ -776,3 +907,6 @@ void gpu_vec_step(StaticVec* vec) {
 void cpu_vec_step(StaticVec* vec) {
-    assert(vec->buffers == 1);
+    if (vec->buffers != 1) {
+        fprintf(stderr, "cpu_vec_step: synchronous stepping requires one buffer\n");
+        abort();
+    }
     _static_vec_env_step(vec);
